/* Author: University of Salento and CMCC */

var form1_controlli = new Array();

Ext.onReady(function () {

	if(!document.all) {
		var basic = new Ext.Resizable('my_chart', {
	        width: 700,
	        height: 350,
	        minWidth:700,
	        minHeight:350,
	        wrap:true,
	        pinned:true,
	        //animate:true,
	        //easing: 'backIn',
	        //duration:.6,
	        preserveRatio: true
		});
	}
});
